# v17 Plan — 71-Pillar Cycle 7 P0

**Date:** 2026-06-21
**Branch:** `chore/v17-71-pillar-cycle-7-p0-2026-06-21`
**Supersedes:** v15, v16
**Cadence:** per ADR-041 (weekly Mon 09:00 PDT)
**Cumulative:** 33 of 47 P0 closed (70%); 9-pillar mean 3.00; 9.5-pillar mean 2.95

## Target

Close 8 more P0 pillars (L1, L2, L3, L4, L8, L10, L11, L12) + L40 + L41.
Target 9-pillar mean 3.00 → 3.00 (saturated), 11.5-pillar mean 2.95 → 3.00.

## Wave A (5 tracks, parallel)

### T1 — L1 Architecture overview per service (3 repos)

Deliverable: 3 × `docs/architecture/<service>.md` files (pheno-config, pheno-port-adapter, pheno-mcp-router).
Each: context, c4 model (Container view), key decisions, future-state.

### T2 — L2 Module boundary doc + dependency table

Deliverable: `docs/architecture/module-boundary.md` (200 lines) listing every fleet crate with:
- module list
- public API surface
- dep graph
- cycle audit (uses `cargo-modules` from L6)

### T3 — L3 Coupling metric enforcement (rust-audit.toml)

Deliverable: `.rust-audit.toml` with max coupling 5 per module. CI step `cargo-audit --coupling-threshold 5`.

### T4 — L4 Hexagonal port pattern enforcement

Deliverable: `docs/architecture/hexagonal-ports.md` + linter `scripts/check-hex-ports.sh` that verifies every adapter implements the `Port` trait. Adopted in 5 substrate repos.

### T5 — L8 Observability hooks in 3 critical crates

Deliverable: 3 × `lib.rs` patches adding `#[tracing::instrument]` on public methods, `OTLP` exporter init, `pheno-tracing` wire-up.

## Wave B (3 tracks, parallel after A)

### T6 — L10 Async runtime choice (tokio vs async-std)

Deliverable: `docs/architecture/async-runtime-decision.md` ADR. Decision: tokio (existing precedent). Add `[[bin]]` `tokio = { version = "1", features = ["full"] }` to 3 crates that still use async-std.

### T7 — L11 Anti-fragility (chaos_ tests in 5 crates)

Deliverable: 5 × `tests/chaos_<name>.rs` files (pheno-port-adapter, pheno-mcp-router, pheno-config, pheno-flags, pheno-errors). Each tests graceful degradation under failure.

### T8 — L12 Type safety (TypeState pattern, serde-denied, missing_docs)

Deliverable: `docs/architecture/type-safety-convention.md` + `#![deny(missing_docs)]` + `#![deny(unsafe_code)]` in 3 critical crate `lib.rs`.

## Wave C (2 tracks, after B)

### T9 — L40 i18n (English baseline + 1 additional locale per repo)

Deliverable: `i18n/{en,es,ja}/<crate>.ftl` for 3 substrate crates (config errors, mcp errors, port errors).

### T10 — L41 a11y (CLI flag descriptions, screen-reader labels)

Deliverable: `scripts/check-a11y.sh` that verifies all CLI flags have `--help` descriptions and `--json` mode for machine consumption. Adopted in 3 CLI tools.

## Wave D — closure

- Cycle 7 probe
- v18 plan
- AGENTS.md refresh
- Final 9-pillar mean 3.00; 11.5-pillar mean 2.95 → 3.00

## Acceptance criteria

- [ ] All 10 tracks shipped
- [ ] 9-pillar mean = 3.00 (perfect)
- [ ] 11.5-pillar mean ≥ 2.95
- [ ] 0 net content loss
- [ ] No new arch debt