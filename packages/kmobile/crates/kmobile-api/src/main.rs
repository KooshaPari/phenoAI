//! KMobile API — HTTP server for remote device/simulator/project management.
//!
//! Serves REST endpoints that delegate to the core ports.

use anyhow::Result;

#[tokio::main]
async fn main() -> Result<()> {
    println!("KMobile API server stub (hexagonal refactor in progress)");
    Ok(())
}
