# cargo-deny configuration for PhenoCompose sub-crates
# See https://embarkstudios.github.io/cargo-deny/

[graph]
all-features = true

[advisories]
version = 2
ignore = []

[licenses]
version = 2
allow = ["MIT", "Apache-2.0", "Apache-2.0 WITH LLVM-exception", "BSD-2-Clause", "BSD-3-Clause", "ISC", "Unicode-DFS-2016", "Unicode-3.0", "Zlib", "CC0-1.0", "MPL-2.0", "0BSD"]

[bans]
multiple-versions = "allow"
wildcards = "allow"
highlight = "all"
deny = []

[sources]
unknown-registry = "deny"
unknown-git = "deny"

# In-tree crate (nvms-ffi C-shim bindings) is an internal tool — it is
# not redistributed and should not require an explicit SPDX license
# field for cargo-deny purposes.
[licenses.private]
ignore = true
