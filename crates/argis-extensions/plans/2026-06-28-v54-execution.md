# v54 — ADR-097 pre-commit minimum + router CI artifacts + fleet scorecard OCI

**Status:** DRAFT — 2026-06-28

**Tracks:** 4

## T1 — Router CI artifacts + scorecard integration (P0)
- Wire phenotype-router into `inventory.sh` and `drift.sh` monitoring scope
- Add phenotype-router to `scripts/cache-stats.sh` repo list
- Run `just ci` on phenotype-router to verify green CI
- Add CI/Deny/Audit badges to phenotype-router README

## T2 — Fleet scorecard as OCI artifact (P1)
- Publish weekly scorecard as an OCI artifact to GHCR (`ghcr.io/kooshapari/fleet-scorecard`)
- Tag with `vX.Y.Z` (cycle version) and `latest`
- Push on each weekly cycle via `pillar-checks.yml`

## T3 — 71-pillar cycle-40 probe (P0)
- Fleet mean check: 3.72, 20 cycles sustained (v32–v54)
- Cycle-40 probe finding
- v54 plan closure
- v55 plan creation

## T4 — ADR-097 pre-commit minimum adoption audit (P1)
- Scan all 20 boarded repos for .pre-commit-config.yaml presence
- For repos missing it: create the file, PR to main
- Verify pre-commit hooks match the fleet standard (ADR-097)

Refs: ADR-097, ADR-096, v53, L31
